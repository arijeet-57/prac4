// interface Employee {
//     name: string;
//     age: number;
//     userId: string;
// }

//Now below is the partial for the above interface
interface Employee {
    name? : string;
    age? : number;
    userId? : string;
}